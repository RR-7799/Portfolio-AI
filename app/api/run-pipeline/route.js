import { NextResponse } from "next/server";
import { GET as runBatchSync } from "../batch-sync-upstox/route";
import { GET as runFreshness } from "../freshness-report/route";
import { GET as runV51 } from "../calculate-score/route";
import { GET as runSnapshot } from "../portfolio-snapshot/route";
import { GET as runAlerts } from "../portfolio-alerts/route";
export const dynamic="force-dynamic";export const maxDuration=300;const ENGINE_VERSION="pipeline_v1_9";const BATCH_SIZE=10;
function isAuthorized(request){const s=process.env.PIPELINE_SECRET;if(!s)return false;const h=request.headers.get("x-pipeline-secret");const a=request.headers.get("authorization");return h===s||(a?.startsWith("Bearer ")&&a.slice(7)===s);}
async function parse(r){try{return await r.json();}catch{return{success:false,error:"Pipeline stage returned non-JSON response",status:r.status};}}
async function stage(label,handler,url,headers={}){const t=Date.now();try{const r=await handler(new Request(url,{method:"GET",headers}));const p=await parse(r);return{stage:label,http_status:r.status,duration_ms:Date.now()-t,success:r.status>=200&&r.status<300&&p?.success!==false,data:p};}catch(e){return{stage:label,http_status:500,duration_ms:Date.now()-t,success:false,data:{success:false,error:e?.message||`${label} failed`}};}}
export async function GET(request){if(!isAuthorized(request))return NextResponse.json({success:false,engine_version:ENGINE_VERSION,error:process.env.PIPELINE_SECRET?"Unauthorized":"PIPELINE_SECRET is not configured"},{status:401});const started=Date.now(),{searchParams}=new URL(request.url),requested=String(searchParams.get("stage")||"all").toLowerCase(),stage=["sync","freshness","score","snapshot","alerts","all"].includes(requested)?requested:"all",origin=new URL(request.url).origin,stages=[],headers={"x-pipeline-secret":process.env.PIPELINE_SECRET||""};try{if(stage==="sync"||stage==="all"){let offset=0,finished=false;while(!finished){const r=await stage(`upstox_sync_${offset}`,runBatchSync,`${origin}/api/batch-sync-upstox?limit=${BATCH_SIZE}&offset=${offset}`);stages.push(r);if(!r.success)return NextResponse.json({success:false,engine_version:ENGINE_VERSION,failed_stage:r.stage,elapsed_ms:Date.now()-started,stages},{status:502});const next=r.data?.batch?.next_offset;finished=next==null||r.data?.batch?.returned===0;if(!finished)offset=Number(next);}}if(stage==="freshness"||stage==="all"){const r=await stage("freshness_audit",runFreshness,`${origin}/api/freshness-report`);stages.push(r);if(!r.success)return NextResponse.json({success:false,engine_version:ENGINE_VERSION,failed_stage:r.stage,elapsed_ms:Date.now()-started,stages},{status:502});}if(stage==="score"||stage==="all"){const r=await stage("ai_scorer_v5_1",runV51,`${origin}/api/calculate-score`,headers);stages.push(r);if(!r.success)return NextResponse.json({success:false,engine_version:ENGINE_VERSION,failed_stage:r.stage,elapsed_ms:Date.now()-started,stages},{status:502});}if(stage==="snapshot"||stage==="all"){const r=await stage("portfolio_snapshot",runSnapshot,`${origin}/api/portfolio-snapshot`,headers);stages.push(r);if(!r.success)return NextResponse.json({success:false,engine_version:ENGINE_VERSION,failed_stage:r.stage,elapsed_ms:Date.now()-started,stages},{status:502});}if(stage==="alerts"||stage==="all")stages.push(await stage("portfolio_alerts",runAlerts,`${origin}/api/portfolio-alerts`,headers));const syncStages=stages.filter(x=>x.stage.startsWith("upstox_sync_")),syncResults=syncStages.flatMap(x=>x.data?.results||[]),score=stages.find(x=>x.stage==="ai_scorer_v5_1"),fresh=stages.find(x=>x.stage==="freshness_audit"),snapshot=stages.find(x=>x.stage==="portfolio_snapshot"),alerts=stages.find(x=>x.stage==="portfolio_alerts"),alertsWarning=alerts&&!alerts.success?(alerts.data?.error||alerts.data?.message||`Alert stage failed with HTTP ${alerts.http_status}.`):alerts?.data?.warning||null;return NextResponse.json({success:true,engine_version:ENGINE_VERSION,stage,elapsed_ms:Date.now()-started,pipeline_summary:{sync_batches:syncStages.length,sync_successful:syncResults.filter(x=>x.success===true).length,sync_failed:syncResults.filter(x=>x.success===false&&x.skipped!==true).length,freshness_completed:Boolean(fresh?.success),scoring_completed:Boolean(score?.success),score_history_completed:Boolean(score?.success),score_history_stored:score?.data?.summary?.scored??null,snapshot_completed:Boolean(snapshot?.success),alerts_completed:Boolean(alerts?.success),alerts_generated:alerts?.data?.new_alerts??null,alerts_candidates:alerts?.data?.generated??null,alerts_warning:alertsWarning,snapshot_users:snapshot?.data?.users??null,scored:score?.data?.summary?.scored??null,buy_candidates:score?.data?.results?.filter?.(x=>x.action==="BUY")?.length??null,average_score:score?.data?.summary?.average_final_ai_score??null},stages});}catch(e){console.error("Portfolio AI pipeline error:",e);return NextResponse.json({success:false,engine_version:ENGINE_VERSION,stage,elapsed_ms:Date.now()-started,error:e?.message||"Unknown pipeline error",stages},{status:500});}}
